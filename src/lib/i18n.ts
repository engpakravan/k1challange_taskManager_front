/* This is Just a Simple Before Implement i18n */
export const i18next = {
    t : (text : string , field ?: {[key : string] : number|string}) : string => {
        return `That Field is Required`
    }
}